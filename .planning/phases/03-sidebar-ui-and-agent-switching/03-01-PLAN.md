---
phase: 03-sidebar-ui-and-agent-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/agent.service.ts
  - src/views/agent-tree-provider.ts
  - src/views/agent-tree-items.ts
  - test/__mocks__/vscode.ts
  - test/unit/agent-tree-provider.test.ts
  - test/unit/agent-tree-items.test.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "TreeDataProvider returns repo group headers at root level, one per configured repo with agents"
    - "TreeDataProvider returns sorted agent items as children of each repo group"
    - "Agent items display agentName as label, truncated prompt or 'Interactive session' as description"
    - "Agent items show correct ThemeIcon with ThemeColor per status (running=green circle, created=gray circle, finished=green check, error=red warning)"
    - "Agents within a repo are sorted by status priority (running > created > finished > error) then alphabetically"
    - "TreeDataProvider refreshes when AgentService fires onDidChangeAgents"
  artifacts:
    - path: "src/views/agent-tree-provider.ts"
      provides: "TreeDataProvider with two-level hierarchy (repo groups -> agent leaves)"
      exports: ["AgentTreeProvider"]
    - path: "src/views/agent-tree-items.ts"
      provides: "AgentTreeItem and RepoGroupItem TreeItem subclasses with status icons"
      exports: ["AgentTreeItem", "RepoGroupItem", "getStatusIcon"]
    - path: "src/services/agent.service.ts"
      provides: "onDidChangeAgents event emitter (addition to existing class)"
      contains: "onDidChangeAgents"
    - path: "test/unit/agent-tree-provider.test.ts"
      provides: "Unit tests for TreeDataProvider hierarchy, sorting, refresh"
    - path: "test/unit/agent-tree-items.test.ts"
      provides: "Unit tests for TreeItem properties, status icons, description"
  key_links:
    - from: "src/views/agent-tree-provider.ts"
      to: "src/services/agent.service.ts"
      via: "constructor injection + onDidChangeAgents subscription; getAll() for root-level repo grouping, getForRepo() for child agents"
      pattern: "agentService\\.getAll|agentService\\.getForRepo|onDidChangeAgents"
    - from: "src/views/agent-tree-items.ts"
      to: "src/models/agent.ts"
      via: "AgentStatus type for icon mapping"
      pattern: "AgentStatus"
---

<objective>
Build the TreeDataProvider core and TreeItem classes for the sidebar agent view, plus add the onDidChangeAgents event to AgentService.

Purpose: Provides the data layer that renders agent tiles grouped by repository in the existing vscode-agentic.agents view. This is the foundation all sidebar interactions depend on.
Output: AgentTreeProvider, AgentTreeItem, RepoGroupItem classes, and updated AgentService with change event.
</objective>

<execution_context>
@/Users/norules/.claude/get-shit-done/workflows/execute-plan.md
@/Users/norules/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sidebar-ui-and-agent-switching/03-CONTEXT.md
@.planning/phases/03-sidebar-ui-and-agent-switching/03-RESEARCH.md

@src/models/agent.ts
@src/services/agent.service.ts
@src/services/repo-config.service.ts
@src/models/repo.ts
@test/__mocks__/vscode.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/models/agent.ts:
```typescript
export type AgentStatus = "created" | "running" | "finished" | "error";
export interface AgentEntry {
  agentName: string;
  repoPath: string;
  status: AgentStatus;
  initialPrompt?: string;
  createdAt: string;
  exitCode?: number;
}
```

From src/services/agent.service.ts:
```typescript
export class AgentService {
  getAll(): AgentEntry[];
  getForRepo(repoPath: string): AgentEntry[];
  getAgent(repoPath: string, agentName: string): AgentEntry | undefined;
  async updateStatus(repoPath: string, agentName: string, status: AgentStatus, exitCode?: number): Promise<void>;
  // NOTE: onDidChangeAgents event does NOT exist yet -- Task 1 adds it
}
```

From src/services/repo-config.service.ts:
```typescript
export class RepoConfigService {
  getAll(): RepoConfig[];
  getForRepo(repoPath: string): RepoConfig | undefined;
}
```

From src/models/repo.ts:
```typescript
export interface RepoConfig {
  path: string;
  stagingBranch: string;
  worktreeLimit: number;
}
```

From test/__mocks__/vscode.ts (existing mocks):
```typescript
export class EventEmitter<T> { event; fire(data: T): void; dispose(): void; }
export class TreeItem { label?; collapsibleState?; constructor(label, collapsibleState?); }
export const TreeItemCollapsibleState = { None: 0, Collapsed: 1, Expanded: 2 };
// NOTE: ThemeIcon and ThemeColor mocks do NOT exist yet -- Task 1 adds them
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentService change event and vscode mock extensions</name>
  <files>src/services/agent.service.ts, test/__mocks__/vscode.ts, test/unit/agent.service.test.ts</files>
  <behavior>
    - Test: AgentService fires onDidChangeAgents after createAgent
    - Test: AgentService fires onDidChangeAgents after deleteAgent
    - Test: AgentService fires onDidChangeAgents after updateStatus
    - Test: AgentService fires onDidChangeAgents after reconcileOnActivation (when changes occur)
    - Test: AgentService does NOT fire onDidChangeAgents on reconcileOnActivation when no changes
  </behavior>
  <action>
    1. Update test/__mocks__/vscode.ts:
       - Add ThemeIcon class mock: `class ThemeIcon { constructor(public id: string, public color?: ThemeColor) {} }` -- needs to store both id and color for test assertions
       - Add ThemeColor class mock: `class ThemeColor { constructor(public id: string) {} }`
       - Add `workspace.updateWorkspaceFolders` mock (vi.fn())
       - Add `workspace.workspaceFolders` as settable array
       - Add `window.createTreeView` mock: vi.fn() returning `{ reveal: vi.fn(), dispose: vi.fn(), onDidChangeVisibility: vi.fn() }`
       - Export all new mocks

    2. Add onDidChangeAgents event to AgentService:
       - Add private `_onDidChangeAgents = new vscode.EventEmitter<void>()`
       - Add public `readonly onDidChangeAgents = this._onDidChangeAgents.event`
       - Add `dispose()` method that calls `this._onDidChangeAgents.dispose()`
       - Fire `this._onDidChangeAgents.fire()` at the end of: createAgent, deleteAgent, updateStatus, and reconcileOnActivation (only when `changed === true`)

    3. Add tests in test/unit/agent.service.test.ts for the new event:
       - Subscribe a vi.fn() listener to onDidChangeAgents
       - Assert it fires after create, delete, updateStatus, reconcile-with-changes
       - Assert it does NOT fire on reconcile-with-no-changes

    Note: The existing 27 AgentService tests must continue passing. The event additions are backward-compatible.
  </action>
  <verify>
    <automated>npx vitest run test/unit/agent.service.test.ts --reporter=verbose</automated>
  </verify>
  <done>AgentService has onDidChangeAgents event that fires on all state mutations. ThemeIcon, ThemeColor, createTreeView, updateWorkspaceFolders mocks available in vscode mock. All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentTreeProvider and TreeItem classes</name>
  <files>src/views/agent-tree-provider.ts, src/views/agent-tree-items.ts, test/unit/agent-tree-provider.test.ts, test/unit/agent-tree-items.test.ts</files>
  <behavior>
    - Test: getChildren(undefined) returns RepoGroupItem per repo that has agents
    - Test: getChildren(undefined) returns empty array when no agents exist
    - Test: getChildren(repoGroup) returns AgentTreeItem[] sorted by status priority then name
    - Test: getChildren(agentItem) returns empty array (leaf nodes)
    - Test: getParent(agentItem) returns RepoGroupItem with matching repoPath
    - Test: getParent(repoGroup) returns undefined
    - Test: RepoGroupItem has id="repo:{repoPath}", contextValue="repoGroup", collapsibleState=Expanded
    - Test: RepoGroupItem label is last segment of repo path
    - Test: AgentTreeItem has id="agent:{repoPath}::{agentName}", contextValue="agentItem", collapsibleState=None
    - Test: AgentTreeItem label is agentName, description is truncated prompt (max 40 chars) or "Interactive session"
    - Test: AgentTreeItem.command triggers vscode-agentic.switchToAgent with [repoPath, agentName]
    - Test: getStatusIcon("running") returns ThemeIcon("circle-filled") with green ThemeColor
    - Test: getStatusIcon("created") returns ThemeIcon("circle-outline") with gray ThemeColor
    - Test: getStatusIcon("finished") returns ThemeIcon("check") with green ThemeColor
    - Test: getStatusIcon("error") returns ThemeIcon("warning") with red ThemeColor
    - Test: refresh() fires onDidChangeTreeData event
    - Test: AgentTreeProvider subscribes to agentService.onDidChangeAgents and calls refresh (debounced)
    - Test: Sorting order: running agents before created before finished before error, then alphabetical within same status
  </behavior>
  <action>
    1. Create src/views/agent-tree-items.ts:
       - Export `getStatusIcon(status: AgentStatus): vscode.ThemeIcon` mapping:
         - running -> ThemeIcon("circle-filled", ThemeColor("testing.iconPassed"))
         - created -> ThemeIcon("circle-outline", ThemeColor("disabledForeground"))
         - finished -> ThemeIcon("check", ThemeColor("testing.iconPassed"))
         - error -> ThemeIcon("warning", ThemeColor("testing.iconFailed"))
       - Export `RepoGroupItem extends vscode.TreeItem`:
         - Constructor takes repoPath
         - label = last path segment (repoPath.split("/").pop() or path.basename)
         - id = `repo:${repoPath}`
         - contextValue = "repoGroup"
         - collapsibleState = Expanded
         - tooltip = full repoPath
         - iconPath = ThemeIcon("repo")
       - Export `AgentTreeItem extends vscode.TreeItem`:
         - Constructor takes agentName, repoPath, status, initialPrompt?
         - label = agentName
         - description = initialPrompt ? truncate(initialPrompt, 40) : "Interactive session"
         - id = `agent:${repoPath}::${agentName}` (stable identity for refresh)
         - contextValue = "agentItem"
         - collapsibleState = None
         - iconPath = getStatusIcon(status)
         - tooltip = `${agentName} (${status})\n${repoPath}` (per Claude's discretion)
         - command = { command: "vscode-agentic.switchToAgent", title: "Focus Agent", arguments: [repoPath, agentName] }
       - Helper: `truncate(str: string, max: number): string` -- truncates with "..." if exceeds max

    2. Create src/views/agent-tree-provider.ts:
       - Export `AgentTreeProvider implements vscode.TreeDataProvider<RepoGroupItem | AgentTreeItem>`:
         - Constructor takes AgentService (RepoConfigService is NOT used -- repo groups are derived from agentService.getAll() grouped by repoPath, since only repos with agents should appear per CONTEXT.md)
         - Private `_onDidChangeTreeData = new vscode.EventEmitter<void>()`
         - Public `readonly onDidChangeTreeData = this._onDidChangeTreeData.event`
         - `refresh()` method fires the event
         - Private `debouncedRefresh` using simple debounce (150ms) that calls refresh()
         - In constructor, subscribe to `agentService.onDidChangeAgents(() => this.debouncedRefresh())`
         - `getTreeItem(element)`: returns the element itself (both extend TreeItem)
         - `getChildren(element?)`:
           - If no element (root): get all agents via agentService.getAll(), group by repoPath using a Map, return RepoGroupItem for each unique repoPath that has agents. Do NOT use repoConfigService -- only repos with agents should appear (welcome content handles the empty state).
           - If RepoGroupItem: get agents for this repo via agentService.getForRepo(element.repoPath), sort by STATUS_PRIORITY then alphabetical, return AgentTreeItem for each
           - If AgentTreeItem: return [] (leaf)
         - `getParent(element)`:
           - If AgentTreeItem: return new RepoGroupItem(element.repoPath)
           - If RepoGroupItem: return undefined
         - Private STATUS_PRIORITY map: { running: 0, created: 1, finished: 2, error: 3 }
         - Private sortAgents method using the priority map
         - `dispose()` method to clean up the event subscription and event emitter

    3. Create test/unit/agent-tree-items.test.ts:
       - Test getStatusIcon for all 4 statuses (check ThemeIcon id and ThemeColor id)
       - Test RepoGroupItem: id, contextValue, label (last segment), collapsibleState, tooltip
       - Test AgentTreeItem: id, contextValue, label, description (with prompt), description (without prompt), collapsibleState, command structure
       - Test truncate: short string unchanged, long string truncated with "..."

    4. Create test/unit/agent-tree-provider.test.ts:
       - Mock AgentService with getAll(), getForRepo(), onDidChangeAgents (use EventEmitter from mock)
       - Test getChildren(undefined) returns RepoGroupItem per repo with agents
       - Test getChildren(undefined) returns empty when no agents
       - Test getChildren(repoGroup) returns sorted AgentTreeItem array
       - Test sorting: running > created > finished > error, alphabetical within status
       - Test getChildren(agentItem) returns empty array
       - Test getParent returns RepoGroupItem for agent, undefined for repo
       - Test refresh fires onDidChangeTreeData
       - Test auto-refresh: fire agentService.onDidChangeAgents -> provider refresh fires (may need to account for debounce delay)
  </action>
  <verify>
    <automated>npx vitest run test/unit/agent-tree-provider.test.ts test/unit/agent-tree-items.test.ts --reporter=verbose</automated>
  </verify>
  <done>AgentTreeProvider returns correct two-level hierarchy with sorted agents per repo. AgentTreeItem shows name, truncated prompt description, status icon, and click command. RepoGroupItem shows repo name with collapsible header. TreeView auto-refreshes on agent state changes. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
# All existing tests (148) + new tests must pass
# TypeScript compiles clean: npx tsc --noEmit
</verification>

<success_criteria>
- AgentTreeProvider.getChildren(undefined) returns one RepoGroupItem per repo that has agents
- AgentTreeProvider.getChildren(repoGroup) returns AgentTreeItem[] sorted by status priority then alphabetical
- AgentTreeItem shows agent name as label, truncated prompt or "Interactive session" as description
- Status icons: running=green circle-filled, created=gray circle-outline, finished=green check, error=red warning
- Agent items have command binding to vscode-agentic.switchToAgent with [repoPath, agentName]
- TreeView refreshes automatically when AgentService state changes
- All 148+ existing tests still pass, new tests pass, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-ui-and-agent-switching/03-01-SUMMARY.md`
</output>
