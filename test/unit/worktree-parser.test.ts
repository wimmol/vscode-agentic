import { describe, expect, it } from "vitest";

describe("parseWorktreeList", () => {
	// Real tests will be added in Plan 02:
	// - single worktree (main only)
	// - multiple worktrees with branches
	// - detached HEAD worktree
	// - locked worktree
	// - prunable worktree
	// - empty output

	it("placeholder: porcelain parser module will be testable", () => {
		expect(true).toBe(true);
	});
});
