export const WORKSPACE_SCOPE_KEY = 'workspace';
export const PERSIST_DEBOUNCE_MS = 500;

// Context values for tree items (used in package.json `when` clauses)
export const CONTEXT_FILE = 'explorerFile';
export const CONTEXT_FOLDER = 'explorerFolder';

// Drag-and-drop MIME types
export const URI_LIST_MIME = 'text/uri-list';
