import type * as vscode from "vscode";

export function activate(context: vscode.ExtensionContext): void {
	// Services and commands will be wired in Plan 03
	void context;
}

export function deactivate(): void {
	// Cleanup if needed
}
