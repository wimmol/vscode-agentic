import * as vscode from 'vscode';

export const activate = (_context: vscode.ExtensionContext) => {
  // TODO: register providers and commands
};

export const deactivate = () => {
  // TODO: cleanup
};
